using VueForm.Core.Entities;
using VueForm.SharedKernel.Interfaces;
using System.Linq;

namespace VueForm.Core
{
    public static class DatabasePopulator
    {
        public static int PopulateDatabase(IRepository repository)
        {           
            return 0;
        }
    }
}
